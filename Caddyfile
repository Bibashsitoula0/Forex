# Reverse proxy + automatic HTTPS (Let's Encrypt) for the XAU/USD app.
# Runs as the system Caddy service on the VPS (see deploy runbook).
# Requires the domain's DNS A record to point at the VPS's public IP
# before Caddy will succeed in issuing a certificate.

bestpainters.ca {
    reverse_proxy localhost:8000
}
